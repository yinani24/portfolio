export interface ParallaxProps {
    children: string;
    baseVelocity: number;
}

export interface TopBarProps{
    children: string;
}

export interface MotionButton{
    children: string;
    Icon: any;
    href: string;
}